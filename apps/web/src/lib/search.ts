/** Debounce delay before updating the shop URL / firing search. */
export const SEARCH_DEBOUNCE_MS = 300;

/** Minimum characters before we query the API (avoids noisy single-char scans). */
export const SEARCH_MIN_CHARS = 2;
